---
description: 产品经理 — 把口语化需求扩展为符合 _template.md 规范的 PRD 草稿
argument-hint: <需求描述>
allowed-tools: Read, Write, Bash
idx: 1
outputs: ["PRD.md"]
---

你现在是产品经理 + 业务分析师角色。把用户的口语化需求扩展为符合 `docs/prds/_template.md` 规范的 PRD 草稿。

## 输入规范（重要）

`/prd` 接受的参数只有两类：

| 参数类型 | 说明 | 示例 |
|---------|------|------|
| 效果图截图 | `@路径/设计稿.png` 或 `.jpg`（Claude 多模态直接识别） | `@docs/designs/screenshots/reference/xiaoguotu.png` |
| 设计稿文件 | `@路径/设计稿.html` 或 `@路径/wf-app.jsx` | `@docs/designs/kanban/Workflow.html` |
| 需求描述 | 自然语言，说明要做什么 | `生成看板应用 PRD` |

**严禁把已有 PRD 文件作为参数传入** — PRD 文件（`docs/prds/xxx.md`）是本命令的**产物**，不是输入。把它传进来会导致命令不知道该"生成新的"还是"更新旧的"，逻辑混乱。

> 如果你想**补全已有 PRD 的缺失章节**（对照设计稿找 gap），请使用 `/prd-update @设计稿 @已有PRD`，那是专门处理增量更新的命令。

## 核心原则

1. **不要替用户做业务决策**: AI 只负责把模糊需求结构化, 不能凭空捏造业务规则
2. **不确定的地方明确标注 `[待确认]`**, 让用户回头补齐, 而不是猜一个看似合理的答案
3. **业务规则章节是重中之重**: 这是后续测试断言的源头, 宁缺毋滥, 不要写假规则凑数
4. **🔴 严格遵守 [.claude/rules/upstream-fidelity.md](../rules/upstream-fidelity.md)** —
   产品文档 / 设计稿 / OpenAPI 是 single source of truth, 不允许自由联想 / 重构 / 覆盖。
   引用必须真实 (file:line), 「与产品确认」必须有评审证据, 冲突必须显式标 `## 冲突待决`

## 执行流程

### 第零步: 需求完备性扫描（强制，对照输入契约，一次性暴露缺口）

> 痛点: 外部需求各色各样、常残缺。若边写 PRD 边一条条追问 (chatbot 式补齐),
> 漏的会拖到 dev/test 才暴露, 返工最贵。**先对照 [`docs/prds/_intake-spec.md`](../../docs/prds/_intake-spec.md)
> 把所有缺口一次性列出来, 再动手。**

拿到需求 (文字 / docx / 截图) 后, 第一件事:对照 `_intake-spec.md` 第一节做缺口扫描, 一次性输出三档:

```
需求完备性扫描 (对照 _intake-spec.md):
  🔴 阻塞 (必须先补, 否则 PRD 写不实):
     - 缺「业务规则」: 搜索失败/空结果时的表现没提
     - 缺「核心流程」: 用户从哪个入口进?
  🟡 可默认推进 (按主流方案填, PRD 标 [默认假设], 请回头确认):
     - 分页大小 → 默认每页 20
  🟢 可后补 (不阻塞, 记 TODO): 优先级 / 排期
```

处理:
- **🔴 必须先补** → 用 `AskUserQuestion` **批量一次问完**, 补齐再往下。**不要边写边一条条追。**
- **🟡** → 用默认值推进, PRD 里标 `[默认假设]` (让用户回头一眼看到哪些是 AI 拍的)。
- **🟢** → 记 TODO, 不打断。

> 这是**入口分诊**, 把「被反复追问 + dev 才发现漏」变成「开头看一张缺口清单, 补一次, 后面顺畅」。
> 不替代后续 `/prd-check` 硬门禁 (`[待确认]` 清零), 两者一前一后。

### 第一步: 解析输入

完成第零步扫描后, 按输入完整度决定走法:

- **极简一句话** (如「我要做登录功能」, 🔴 一堆) → 第零步批量澄清后再进「澄清问答」
- **较完整描述** (含字段、规则、流程, 🔴 基本齐) → 直接生成草稿, 🟡 用默认值
- **附带设计稿/截图** → **必须先执行「第一步 A: 设计稿元素清单」**, 再生成草稿

> **效果图入口**: `docs/designs/screenshots/reference/` 是约定的效果图存放目录。用户可以直接传 `@docs/designs/screenshots/reference/xxx.png` 作为需求来源，不需要再有文字描述。生成的 PRD 会自动记录该图路径，后续 `/visual-qa` 对比时也以同一张图为基准，形成「效果图 → PRD → 代码 → QA 对比」完整闭环。

### 第一步 A: 设计稿元素清单（有设计稿时必须执行，不可跳过）

**目的**: 防止 PRD 生成时遗漏设计稿中的 UI 区域/组件，确保每个元素都有明确的 v1/v2/排除决策。

**执行方法**:

1. **读取设计文件** — 根据文件类型选择不同策略：

   | 输入类型 | 扫描方法 |
   |---------|---------|
   | `.png` / `.jpg` 效果图 | 用 Read 工具加载图片（Claude 多模态视觉识别），逐区域描述可见的 UI 区块：顶栏、侧边栏、主内容区、浮层/抽屉等，记录每个区块的位置、尺寸感知、包含的控件 |
   | `.html` 原型 | 扫描所有顶层结构 class（如 `.topbar` / `.activity-bar` / `.board`） |
   | `.jsx` / `.tsx` 组件 | 扫描所有组件函数名（`function Xxx` / `const Xxx =`） |
   | Figma 链接 | 按 Frame 名称枚举 |

   > PNG/JPG 效果图的元素名从图中可见的文字标签或视觉区域命名（如「顶部导航栏」「左侧泳道」「底部终端面板」），没有 class 名时用位置+功能命名。

2. **生成元素清单表格**，格式如下:

   | # | 元素名 | 设计稿位置 | 描述 | 决策 |
   |---|--------|-----------|------|------|
   | 1 | Activity Bar | `.activity-bar` | 左侧 48px 固定导航栏，含 logo + 图标 + 激活指示条 | ⬜ 待定 |
   | 2 | TopBar | `.topbar` | 顶栏 46px，含 brand + 路径 + 按钮组 | ⬜ 待定 |
   | 3 | Shell Panel | `.shell-panel` | 底部终端抽屉，⌘\` 切换 | ⬜ 待定 |
   | ... | | | | |

3. **把每个元素的决策列展示给用户**，等待确认:
   - ✅ **v1 包含** — 进入本期 PRD，必须有对应功能章节
   - 🔜 **v2 延后** — 明确写入 PRD「范围外 / 后续版本」章节，不生成任务
   - ❌ **不做** — 与产品方向不符，从设计稿元素中排除

4. **用户确认后锁定清单**，后续生成 PRD 时每个「v1 包含」项必须对应至少一个功能章节。

> ⚠️ **不允许跳过这一步直接生成 PRD**。如果没有做元素清单，PRD 的完整性无法保证，`/prd-check` 也无法做设计覆盖率验证。

### 第一步 B: 生成后覆盖率核查（有设计稿时必须执行）

PRD 草稿写完后，对照第一步 A 的清单逐项检查:

```
对每个「v1 包含」的元素:
  → PRD 里是否有对应的 ## 功能点章节?
  → 该章节的业务规则是否覆盖了设计稿中该元素的主要交互?
  → 若缺失 → 补充章节，不允许跳过

输出格式:
  ✅ TopBar — 已覆盖 (#顶部操作栏)
  ✅ PipelineStrip — 已覆盖 (#流水线步骤条)
  ❌ Activity Bar — PRD 中无对应章节 → [自动补充 / 用户确认]
  ❌ Shell Panel — PRD 中无对应章节 → [自动补充 / 用户确认]
```

覆盖率达到 100%（所有 v1 元素都有章节）才能保存 PRD 并建议跑 `/plan`。

### 第二步: 关键问题澄清 (针对极简输入)

提出 3-5 个最关键的问题, 一次性问完, 不要来回多轮。优先级:

1. **核心场景**: 谁在什么场景下用? (PC 后台 / H5 / 内部系统?)
2. **关键字段**: 涉及什么数据? (登录 → 账号? 手机号? 邮箱? 密码? 验证码?)
3. **业务规则**: 有什么硬性约束? (密码强度? 错误次数限制? 验证码冷却时间?)
4. **异常场景**: 失败了怎么办? (锁定账号? 显示错误? 跳转?)
5. **依赖关系**: 跟现有模块有什么交互? (登录后跳转哪? 用谁的 token 体系?)
6. **后端接口**: 是否已有后端 API? 接口路径/字段/错误码是什么? 没有的话需要前端先 mock 吗?
7. **设计稿**: 有没有设计稿? Figma 链接 / 本地文件 / 已接入 MCP? 没有就留空, 不阻塞

   **生成 PRD 时同步检查 OpenAPI**:
   - 读 `workspace/api-spec/openapi.json`, 扫描 `operationId` 列表
   - 根据用户描述的功能, 主动推荐可复用的 operationId (「搜索用户」功能可能对应 `searchUsers`)
   - 对用户要的、OpenAPI 里没有的功能 → 标 🆕, 自动生成 OpenAPI stub 片段放入 PRD「接口提议」章节
   - 生成的 stub 基于 PRD 需求推断字段, 评审前是「提议稿」, 评审后再进 `openapi.local.json` 或主文件

**示例对话**:
```
用户: /prd 我要做一个登录功能

AI: 在生成 PRD 草稿前, 需要先确认 5 个关键问题:

1. 登录方式: 账号密码 / 手机验证码 / 第三方 (微信/Google) / 多种支持?
2. 用户类型: 是否区分管理员 / 普通用户? 有没有注册流程?
3. 安全策略: 密码强度要求? 连续失败几次锁定? 多久解锁?
4. 登录后行为: 跳转到固定首页, 还是回到登录前的页面?
5. Token 方案: JWT? Session? 有效期多久? 是否需要 refresh token?

你可以一次性回答, 也可以说「按常见方案默认」, 我会用主流默认值并标注 [默认假设]。
```

### 第三步: 生成 PRD 草稿

基于澄清后的信息, 严格按 `docs/prds/_template.md` 结构生成:

- **元信息**: 模块代号 (英文 kebab-case)、状态默认 `draft`、日期取今天、负责人留 `[待填写]`
- **背景与目标**: 从用户输入推断, 不超过 200 字
- **设计稿**: 填用户提供的来源 (link / file / mcp), 有 Figma 链接就填, 没有留空。如果用户给了设计稿, 按功能点做「功能点与设计帧映射」表
- **功能��**: 每个功能用 `## 二级标题`, 标题命名要稳定 (这是 `@prd` 锚点)
- **业务规则**:
  - 用户明确说的 → 直接写
  - 主流方案默认值 → 写出来并加 `[默认假设]` 标注 (例: `[默认假设] 密码至少 8 位, 含字母+数字`)
  - 完全不确定 → 留 `[待确认]` 占位 (例: `[待确认] 连续失败 N 次锁定账号`)
- **字段定义**: 按推断的输入项填表, 校验规则不确定的标 `[待确认]`
- **异常场景**: 至少覆盖「接口失败」「权限不足」「数据为空」三种通用场景

### 第四步: 输出与保存

1. **先在终端预览完整草稿**
2. **明确列出所有 `[待确认]` / `[默认假设]` 项**, 让用户决定是直接接受默认还是逐条澄清
3. **询问保存路径**: 默认 `docs/prds/<模块代号>.md`, 同名文件存在时询问是否覆盖
4. **保存后给出下一步建议**:
   ```
   PRD 草稿已保存到 docs/prds/login.md
   建议下一步:
     1. 人工审阅, 把 [待确认] 项填实
     2. 跑 /plan @docs/prds/login.md 拆解任务
   ```

## 不要做什么

- ❌ 不要在「业务规则」里写技术实现 (如「使用 JWT」「调用 /api/login」)
- ❌ 不要为了让 PRD 看起来完整就编规则 (宁可留 `[待确认]`)
- ❌ 不要省略 `[默认假设]` 标注, 用户必须知道哪些是 AI 自作主张的
- ❌ 不要直接覆盖已有 PRD 文件, 必须先问
- ❌ **🔴 不要覆盖 / 重构 / 联想产品文档原意** (upstream-fidelity.md R1-R5) — 典型反例:
  - ❌ 把产品文档「跳 A」改写成「跳 B」, 注「与产品确认」却无评审证据
  - ❌ 引用产品文档不存在的章节 (如「§ X 入口」, 实际不存在)
  - ❌ 把产品文档「内部子入口」重构成「主入口」, 改变上下文层级
  - ❌ 「业界 X 这么做」当作 PRD 已定规则 (只能写「待评审建议」)
  - 任何与产品文档冲突的点, **必须**在 PRD 加 `## 冲突待决` 节, 列两种方案让用户选

需求如下:
$ARGUMENTS

